# files containing functions, variables and classes
# used to break down large programs into small manageable programs
# promotion of reusability

import operations as op
from operator import add # this is if you only want function add

print(op.add(10,20))
print(op.subtract(10,20))
print(op.multiply(10,20))
print(op.divide(10,20))